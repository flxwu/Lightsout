<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jimdo.hanhan.lightsout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLevel"  />
        <activity android:name=".LoseScreen"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".Home"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LevelChooser"
            android:label="@string/title_activity_level_chooser"
            android:screenOrientation="portrait"
            android:theme="@style/LevelChooserTheme"
        />
        <activity android:name=".WinScreen">
            android:screenOrientation="portrait"
        </activity>
    </application>

</manifest>